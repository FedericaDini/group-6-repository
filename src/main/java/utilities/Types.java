package utilities;

public class Types {
    public enum UserType {
        CUST,
        EMP,
        ADMIN
    }

    public enum OrderState {
        OPENED,
        IN_TRANSIT,
        CLOSED
    }
}
