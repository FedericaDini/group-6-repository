package it.unipi.lsdb.utilities;

public class Types {
    public enum UserType {
        CUSTOMER,
        EMPLOYEE,
        ADMIN
    }

    public enum OrderState {
        OPENED,
        IN_TRANSIT,
        CLOSED
    }
}
