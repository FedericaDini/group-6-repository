package utilities;

public class Types {
    public enum UserType {
        CUSTOMER,
        EMP,
        ADMIN
    }

    public enum OrderState {
        OPENED,
        IN_TRANSIT,
        CLOSED
    }
}
